name: Generate Postgres Lockfile

on:
  push:
    branches:
      - feat/postgres-runtime

permissions:
  contents: write

jobs:
  update-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/postgres-runtime

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install PostgreSQL runtime dependencies
        run: |
          npm install pg@8.23.0 --save-exact
          npm install --save-dev @types/pg@8.23.1 --save-exact

      - name: Commit generated package files
        run: |
          if git diff --quiet package.json package-lock.json; then
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "build: adiciona driver PostgreSQL"
          git push origin HEAD:feat/postgres-runtime
